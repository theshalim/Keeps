@model IEnumerable<Keeps.Models.Country>
@{
    ViewData["Title"] = "Index";
}
<div class="row">
    <div class="col-lg-8">
        <a asp-action="Create" class="btn btn-info" style="float:right">Create</a>
        <br />

        <table class="table table-bordered table-hover">
            <thead>
                <tr><th>SL.</th>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var data in Model)
                {
                    <tr><td></td>
                        <td>@Html.DisplayFor(modeldata => data.Name)</td>
                        <td>
                         <a asp-action="Edit" >Edit</a>
                            <a asp-action="Delete" class="btn btn-outline-danger">Delete</a>
                            
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
</div>